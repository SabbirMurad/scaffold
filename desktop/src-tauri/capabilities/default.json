{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main Scaffold window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
